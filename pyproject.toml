[project]
name = "filter-csv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.2.1",
    # pytest libraries
    "pytest>=8.4.1",
    "pytest-clarity>=1.0.1",
    "pytest-cov>=6.2.1",
    "pytest-sugar>=1.0.0",
    # pretty csv data output
    "tabulate>=0.9.0",
]
